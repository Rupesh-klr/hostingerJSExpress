{
 "name": "server",
 "main": "index.js",
 "dependencies": {
  "express": "^4.21.0",
  "http-proxy-middleware": "^3.0.5",
  "path-to-regexp": "^8.2.0",
  "serve-static": "^1.16.0"
 },
 "scripts": {
  "start": "node index.js ",
  "build": "rm -rf dist && mkdir dist && cp index.js package.json dist/ && echo 'Build Successful' > dist/build-log.txt",
  "start:prod": "cross-env NODE_ENV=production node dist/index.js"
 },
 "devDependencies": {
  "cross-env": "^7.0.3"
 }
}
